services:

  llama-server:
    image: ${LLAMACPP_IMAGE}

    container_name: llama-server

    restart: unless-stopped

    ports:
      - "${LLAMACPP_PORT}:8080"

    command: >
      --host 0.0.0.0 --port 8080
